<% content_for(:breadcrumb) do %>
  <li class="breadcrumb-item"><a href="javascript:void(0)">Dashboard</a></li>
  <li class="breadcrumb-item active"><a href="javascript:void(0)">Suburbs</a></li>
<% end %>

<% content_for(:content) do %>

<div class="row">
  <div class="col-lg-12">
    <div class="card min-vh-100">
      <div class="card-body">
        <h1 class="card-title">Suburbs</h1>
          <%= link_to "New suburb", new_suburb_path, class: 'btn btn-success text-white' %>
          <table class="table table-sm">

            <thead>
              <th>#</th>
              <th>Name</th>
              <th>Description</th>
              <th>Action</th>
            <thead>

            <tbody>
                <% @suburbs.each_with_index do |suburb, index| %>
                  <tr>
                    <td><%= index + 1 %></td>
                    <td><%= suburb.name %></td>
                    <td><%= suburb.description %></td>
                    <td>
                      <%= link_to "View", suburb, class: 'btn btn-success text-white' %>
                      <%= link_to "Edit", edit_suburb_path(suburb), class: 'btn btn-primary' %>
                      <%#= link_to "Delete", suburb, class: 'btn btn-danger', method: :delete %>
                      <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modal<%= suburb.id %>">
                        Delete
                      </button>
                      <div>
                        <!-- Modal -->
                        <div class="modal fade" id="modal<%= suburb.id %>" tabindex="-1" role="dialog" aria-labelledby="modal<%= suburb.id %>Title" aria-hidden="true">
                          <div class="modal-dialog" role="document">
                            <div class="modal-content">
                              <div class="modal-header">
                                <h5 class="modal-title" id="exampleModalLongTitle">Delete Confirmation</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                  <span aria-hidden="true">&times;</span>
                                </button>
                              </div>
                              <div class="modal-body">
                                Are you sure you want to delete this suburb?(<strong><%= suburb.name %></strong>)
                              </div>
                              <div class="modal-footer">
                                <button type="button" class="btn text-white btn-secondary" data-dismiss="modal">Close</button>
                                <%= link_to "Delete", suburb, class: 'btn btn-danger', method: :delete %>
                              </div>
                            </div>
                          </div>
                        </div>
                      </div>
                    </td>
                  </tr>
                <% end %>
            </tbody>  
          </table>
      </div>
    </div>
  </div>
</div>

<% end %>
